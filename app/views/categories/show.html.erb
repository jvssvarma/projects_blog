<h1 align='center'><strong><%= "Category: " + @category.name %></strong></h1>

<div align="center">
	<% if logged_in? and current_user.admin? %>
	<div class="row">
	   <span class="badge"><%= link_to "Edit Category Name", edit_category_path(@category) %></span>
    </div>
    <% end %>
	<%= will_paginate @category_projects %>
</div>
<%= render 'projects/projects', obj: @category_projects%>
<div align="center">
	<%= will_paginate @category_projects %>
</div>
